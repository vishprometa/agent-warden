apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentwarden.fullname" . }}
  labels:
    {{- include "agentwarden.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentwarden.selectorLabels" . | nindent 4 }}
